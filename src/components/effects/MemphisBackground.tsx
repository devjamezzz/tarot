/**
 * Retired with the pastel theme. The single dark theme uses
 * `ConstellationBackground` (mounted in `app/layout.tsx`). Kept as a no-op
 * so any stale import keeps compiling.
 */
export function MemphisBackground(): null {
  return null;
}
